// components/RobotConfig.jsx
const robotParameters = {
    speed: 1, // Cells per second
    rotationSpeed: 90, // Degrees per second (for future use)
    waitTime: 2000, // Milliseconds to wait at the station
  };
  
  export default robotParameters;
  